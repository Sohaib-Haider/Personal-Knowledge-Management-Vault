# Project Rules

## Running opencode

- Always launch opencode from **inside this directory (`pkm/`)**.
- `pkm/` is the single project root and the sole git repository for the vault.
- There is no git repository above this directory; treat the parent folder only as a container.
- Custom opencode agents for this vault live in `.opencode/agents/` under this directory.
